---
title: Exit Intent
sidebar_label: Exit Intent
---

import { Play, Image, FileCode, BookOpen } from 'lucide-react';

Exit intent shows a popup when the visitor moves their mouse to leave the page. Use it to show a last-chance offer, capture an email, or apply a coupon before the visitor closes the tab.

## How It Works

1. The page calls `next.exitIntent({...})` after the SDK has initialized.
2. On desktop, the enhancer listens for `mouseout` events on `<html>` with `clientY <= 10`. Crossing that line fires the popup once.
3. On mobile (where there is no mouse), the enhancer is **disabled by default**. Opt in with `mobileScrollTrigger: true` to fire when the visitor scrolls past 50% of the page instead.
4. After the popup is dismissed once, it does not fire again in the same session — the dismissal is recorded in sessionStorage.

## Two Display Modes

| Mode | When to use |
|---|---|
| **Image mode** | A single image (a coupon banner, a discount graphic). The whole image is clickable and triggers your action. |
| **Template mode** | Custom HTML inside a `<template>` tag. Use action buttons to apply coupons, run a custom function, or close the modal. |

<Cards>
  <Card
    href="/docs/campaigns/utilities/exit-intent/get-started"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-blue-500/15 text-blue-500"><Play className="size-4" /></div>}
    title="Get Started"
    description="Show your first exit intent popup with one method call."
  />
  <Card
    href="/docs/campaigns/utilities/exit-intent/image-mode"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-orange-500/15 text-orange-500"><Image className="size-4" /></div>}
    title="Image Mode"
    description="Single-image popups with a click-through action."
  />
  <Card
    href="/docs/campaigns/utilities/exit-intent/template-mode"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-purple-500/15 text-purple-500"><FileCode className="size-4" /></div>}
    title="Template Mode"
    description="Custom HTML templates with action buttons for coupons, custom callbacks, and closing."
  />
  <Card
    href="/docs/campaigns/utilities/exit-intent/reference"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-slate-500/15 text-slate-500"><BookOpen className="size-4" /></div>}
    title="Reference"
    description="API methods, template attributes, and emitted events."
  />
</Cards>
