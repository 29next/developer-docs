---
title: Debugger
sidebar_label: Debugger
---

import { Play, Terminal, BookOpen } from 'lucide-react';

The debugger is an in-page overlay and console API for inspecting SDK state, monitoring events, and triggering cart operations during development. It is **not** loaded in production builds — you opt in per page via a URL parameter, meta tag, or config flag.

## How It Works

1. The page is loaded with `?debugger=true` (or with `<meta name="next-debug" content="true">`).
2. The SDK lazy-loads the debug module, attaches an overlay to the bottom of the page, and exposes a global `nextDebug` object on `window`.
3. The overlay shows panels for cart, campaign, config, checkout, events, and storage. The console object lets you trigger SDK actions without writing custom code.
4. Refreshing the page without the parameter unloads the debugger entirely.

## Panels

The overlay has six panels:

- **Cart** — current items, totals, discounts; add/remove/update from buttons
- **Campaign** — packages and campaign metadata; sort, inspect, test shipping
- **Config** — active SDK config and feature flags
- **Checkout** — form state and validation errors
- **Event Timeline** — live feed of SDK events with payloads
- **Storage** — `localStorage` and `sessionStorage` browser; edit and delete keys

<Cards>
  <Card
    href="/docs/campaigns/utilities/debugger/get-started"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-blue-500/15 text-blue-500"><Play className="size-4" /></div>}
    title="Get Started"
    description="Enable the debugger and find the overlay."
  />
  <Card
    href="/docs/campaigns/utilities/debugger/console-api"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-purple-500/15 text-purple-500"><Terminal className="size-4" /></div>}
    title="Console API"
    description="Use nextDebug.* methods from the browser console for cart, campaign, attribution, and order operations."
  />
  <Card
    href="/docs/campaigns/utilities/debugger/reference"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-slate-500/15 text-slate-500"><BookOpen className="size-4" /></div>}
    title="Reference"
    description="Complete table of every nextDebug method and property."
  />
</Cards>
