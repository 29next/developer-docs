---
title: Order Data
sidebar_label: Order Data
---

import { Play, ListTree, FileCode, BookOpen } from 'lucide-react';

Order data attributes display post-purchase order information on confirmation, upsell, and receipt pages. The order is loaded automatically when the URL contains `?ref_id=ORDER_ID`.

## How It Works

1. The page loads with `?ref_id=ORD-1234` in the URL.
2. The SDK detects the parameter, fetches the order from the API, and stores it in `orderStore`.
3. Every `data-next-display="order.<property>"` element on the page populates with the live data.
4. CSS classes (`next-loading`, `next-loaded`, `next-error`) reflect the load state.

## Quick Example

```html
<div data-next-show="order.exists">
  <h1>Thank you, <span data-next-display="order.customer.firstName">Friend</span>!</h1>

  <p>Order #<span data-next-display="order.number">-</span></p>
  <p>Total: <span data-next-display="order.total">$0.00</span></p>

  <address data-next-display="order.shippingAddress.full">-</address>
</div>
```

<Cards>
  <Card
    href="/docs/campaigns/data-attributes/order-data/get-started"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-blue-500/15 text-blue-500"><Play className="size-4" /></div>}
    title="Get Started"
    description="A working order confirmation page in 30 lines."
  />
  <Card
    href="/docs/campaigns/data-attributes/order-data/properties"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-orange-500/15 text-orange-500"><ListTree className="size-4" /></div>}
    title="Properties"
    description="All order, customer, address, financial, and attribution properties."
  />
  <Card
    href="/docs/campaigns/data-attributes/order-data/order-items-template"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-purple-500/15 text-purple-500"><FileCode className="size-4" /></div>}
    title="Order Items Template"
    description="Use data-next-order-items to render line items with a template."
  />
  <Card
    href="/docs/campaigns/data-attributes/order-data/reference/attributes"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-slate-500/15 text-slate-500"><BookOpen className="size-4" /></div>}
    title="Reference"
    description="All order.* paths in one searchable table."
  />
</Cards>
