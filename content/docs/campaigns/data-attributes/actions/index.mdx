---
title: Action Attributes
sidebar_label: Actions
---

import { Play, ShoppingCart, Hash, BookOpen } from 'lucide-react';

Action attributes turn any HTML element into an interactive cart control. Add `data-next-action` to a button, link, or div and the SDK takes over: it manages click handling, loading state, error recovery, and accessibility — no JavaScript required.

## How It Works

1. The SDK scans for elements with `data-next-action` (and related action attributes).
2. It instantiates the matching enhancer (`AddToCartEnhancer`, `CartToggleEnhancer`, etc.) and binds a click handler.
3. On click, the enhancer reads the element's other `data-next-*` attributes (package ID, quantity, redirect URL, etc.) and dispatches the action.
4. While the action is in flight, the enhancer applies CSS classes (`next-loading`, `next-disabled`) and disables the element.
5. After the action completes, the enhancer updates the element's text and class state to reflect the new cart state.

## Quick Example

```html
<button
  data-next-action="add-to-cart"
  data-next-package-id="42"
  data-next-quantity="1">
  Add to Cart
</button>
```

That is the full setup. The SDK handles the click, the API call, the loading state, and the cart update.

<Cards>
  <Card
    href="/docs/campaigns/data-attributes/actions/get-started"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-blue-500/15 text-blue-500"><Play className="size-4" /></div>}
    title="Get Started"
    description="A working add-to-cart button in three lines of HTML."
  />
  <Card
    href="/docs/campaigns/data-attributes/actions/cart-actions"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-orange-500/15 text-orange-500"><ShoppingCart className="size-4" /></div>}
    title="Cart Actions"
    description="add-to-cart, toggle, remove, clear, swap — every action that changes cart contents."
  />
  <Card
    href="/docs/campaigns/data-attributes/actions/quantity-actions"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-green-500/15 text-green-500"><Hash className="size-4" /></div>}
    title="Quantity Actions"
    description="Increase, decrease, and direct quantity input controls."
  />
  <Card
    href="/docs/campaigns/data-attributes/actions/reference/attributes"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-slate-500/15 text-slate-500"><BookOpen className="size-4" /></div>}
    title="Reference"
    description="Every action attribute, value, and modifier."
  />
</Cards>

## See Also

- [Cart System](/docs/campaigns/cart-system) — full guide to building cart UIs with these attributes
- [Upsells](/docs/campaigns/upsells) — `data-next-action="accept-upsell"` for post-purchase flows
- [Selection](/docs/campaigns/data-attributes/selection) — selector containers and cards
