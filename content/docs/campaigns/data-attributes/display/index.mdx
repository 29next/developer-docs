---
title: Display Attributes
sidebar_label: Display
---

import { Play, ListTree, ShoppingCart, Package, BookOpen } from 'lucide-react';

Display attributes bind HTML elements to live data. The element's text content updates automatically whenever the underlying data changes — no JavaScript, no DOM manipulation, no event handlers.

The primary attribute is `data-next-display`. It takes a path expression like `cart.total` or `package.42.price` and writes the resolved value into the element's text content.

## Quick Example

```html
<!-- Cart total — updates whenever cart changes -->
<span data-next-display="cart.total">$0.00</span>

<!-- Package price — updates when currency or discount changes -->
<span data-next-display="package.42.price">$0.00</span>

<!-- Customer name from a loaded order -->
<span data-next-display="order.customer.firstName">Friend</span>
```

The fallback text inside the element is shown until the SDK initializes — that's the **progressive enhancement** pattern.

<Cards>
  <Card
    href="/docs/campaigns/data-attributes/display/get-started"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-blue-500/15 text-blue-500"><Play className="size-4" /></div>}
    title="Get Started"
    description="Bind your first display element in 30 seconds."
  />
  <Card
    href="/docs/campaigns/data-attributes/display/paths"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-orange-500/15 text-orange-500"><ListTree className="size-4" /></div>}
    title="Display Paths"
    description="cart.*, package.*, campaign.*, order.*, profile.*, selection.* — all the namespaces you can read."
  />
  <Card
    href="/docs/campaigns/data-attributes/display/cart-items-template"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-green-500/15 text-green-500"><ShoppingCart className="size-4" /></div>}
    title="Cart Items Template"
    description="Use data-next-cart-items to render every cart line with a template."
  />
  <Card
    href="/docs/campaigns/data-attributes/display/package-display"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-purple-500/15 text-purple-500"><Package className="size-4" /></div>}
    title="Package Display"
    description="Display package data inside a parent context with shorthand syntax."
  />
  <Card
    href="/docs/campaigns/data-attributes/display/reference/attributes"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-slate-500/15 text-slate-500"><BookOpen className="size-4" /></div>}
    title="Reference"
    description="Every display attribute and modifier in one searchable table."
  />
</Cards>
