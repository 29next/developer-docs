---
title: State Attributes
sidebar_label: State
---

import { Play, Sigma, ListTree, BookOpen } from 'lucide-react';

State attributes let you control element visibility based on cart, package, order, profile, and URL parameter state — declaratively, in HTML, with no JavaScript.

The two primary attributes are `data-next-show` and `data-next-hide`. They take an expression that is re-evaluated automatically every time the underlying data changes, and they toggle visibility via CSS classes (no DOM thrashing).

## Quick Example

```html
<button data-next-hide="cart.isEmpty">
  Proceed to Checkout
</button>

<div data-next-show="cart.total > 100">
  Free shipping unlocked!
</div>

<div data-next-show="cart.hasItem(123) && !cart.hasItem(456)">
  Add the matching accessory
</div>
```

<Cards>
  <Card
    href="/docs/campaigns/data-attributes/state/get-started"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-blue-500/15 text-blue-500"><Play className="size-4" /></div>}
    title="Get Started"
    description="Show and hide elements based on cart state in five lines of HTML."
  />
  <Card
    href="/docs/campaigns/data-attributes/state/operators"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-green-500/15 text-green-500"><Sigma className="size-4" /></div>}
    title="Operators"
    description="Comparison, logical, and grouping operators for compound conditions."
  />
  <Card
    href="/docs/campaigns/data-attributes/state/properties"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-orange-500/15 text-orange-500"><ListTree className="size-4" /></div>}
    title="Properties"
    description="All cart, package, order, profile, and param properties you can read."
  />
  <Card
    href="/docs/campaigns/data-attributes/state/reference/attributes"
    icon={<div className="size-9 rounded-md flex items-center justify-center bg-slate-500/15 text-slate-500"><BookOpen className="size-4" /></div>}
    title="Reference"
    description="Full attribute and operator reference."
  />
</Cards>
